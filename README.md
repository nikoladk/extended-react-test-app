# Extended React Test App

A React application with intentionally broken Playwright tests for AI training purposes


## Running the App

```bash
npm install
npm start
```

## Running Tests

```bash
npx playwright test
```

## Test Structure

The tests in the `tests/` folder contain intentionally broken locators that need to be fixed.
